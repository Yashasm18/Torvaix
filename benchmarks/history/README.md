# Benchmark History

This directory stores timestamped benchmark snapshots for cross-release comparison.

Each file follows the naming convention:
```
benchmark_v{version}_{timestamp}.json
```

To run benchmarks:
```bash
npm run benchmark
```
